type Query {
    course(String: ID!): Course,
    courses: Courses,
}

type Courses {
    courses: [Course],
}

type Course @key(fields: "id") {
    id: ID!,
    name: String,
    category: String,
}
